source 'http://rubygems.org'

gem 'coveralls',    require: false
gem 'rubocop',      require: false
gem 'transpec',     require: false

# keep this until Rugged::Repository#checkout_index is merged into upstream and released as gem
gem 'rugged',       github: 'niamster/rugged', submodules: true

gemspec
